# spam-sms-classifier
This project builds a machine learning model to detect SMS spam using Natural Language Processing (NLP). It involves data cleaning, text preprocessing, and feature extraction, followed by training a classification model to distinguish between spam and legitimate messages, helping automate spam filtering.
